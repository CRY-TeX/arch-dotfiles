# less history file
export LESSHISTFILE="-"

# change oh-my-zsh default directory
export ZSH=$HOME/.config/.oh-my-zsh

# change the default zsh config path
export ZDOTDIR=$HOME/.config/zsh

# set less search to be case insensitive
LESS=-Ri
